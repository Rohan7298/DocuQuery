services:
  - type: web
    name: docuquery
    env: python
    pythonVersion: 3.11.0  # Use compatible Python version
    buildCommand: |
      apt-get update && apt-get install -y build-essential python3-dev
      pip install --upgrade pip
      pip install -r requirements.txt

      startCommand: gunicorn app:app
